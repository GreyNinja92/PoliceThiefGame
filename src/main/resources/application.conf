ORIGINAL_GRAPH="original.json"
PERTURBED_GRAPH="perturbed.json"
DIFFERENCE_YAML="difference.yaml"

app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  }
}